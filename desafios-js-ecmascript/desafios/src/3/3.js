import getWriterFor from "../writeOutput";

const writeOutput = getWriterFor(3)(3);

function q3() {
  // Write a function that will query the pokeapi for the pokemon with ids 1,4,7 and return the name of the
  // heaviest of the three
}

export default q3;
