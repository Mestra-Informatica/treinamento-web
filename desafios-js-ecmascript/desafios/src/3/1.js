import getWriterFor from "../writeOutput";

const writeOutput = getWriterFor(3)(1);

function q1() {
  // Write a function that will query the pokeapi (https://pokeapi.co/api/v2/pokemon) and return the name of the pokemon
  // with id 4
  return null;
}

export default q1;
