import getWriterFor from "../writeOutput";

const writeOutput = getWriterFor(1)(2);

function q2() {
  // Dado o seguinte Array
  const a = [1, 2, 3, 4, 5, 6];
  // select the first three elements into new variables `b`, `c`, and `d` and the remaining elements in the array into a new variable `rest`
}

export default q2;
