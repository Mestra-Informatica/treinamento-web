import getWriterFor from "../writeOutput";

const writeOutput = getWriterFor(1)(1);

function q1() {
  // Dado o seguinte array
  const a = [1, 2, 3];
  // crie um novo Array `b` de `a` e adicione os números 4, 5, e 6 para que o resultado da seguinte operação seja verdadeiro: `a !== b`
}

export default q1;
