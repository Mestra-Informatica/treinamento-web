import getWriterFor from "../writeOutput";

const writeOutput = getWriterFor(1)(3);

function q3() {
  const a = Array.from(new Array(100), (val, index) => index + 1); // Array [1,2...,100]
  //return a new array with only the even numbers from this array
}

export default q3;
